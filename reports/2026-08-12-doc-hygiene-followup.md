---
disclaimer: >
  This document was generated by automated static heuristic analysis of a
  codebase snapshot. No statement, classification, or recommendation in this
  report should be taken as ground truth. Every doc lifecycle classification
  is a hypothesis derived from file-pattern matching, git metadata, and
  structural inference — not from semantic understanding of content accuracy.
  Findings not backed by a concrete file reference, git date, or verifiable
  structural comparison may be inaccurate or incomplete. Review all
  quarantine proposals and sync verdicts manually before acting on them.
generated_by: "Claude Opus 5 (1M context) via Claude Code — doc-hygiene skill"
date: "2026-08-12"
---

# Doc-Hygiene Report — whatsapp-agent (follow-up pass)

**Generated**: 2026-08-12T14:20:00-03:00
**Scope**: Full repo (hidden directories excluded per skill §6; `.repo/`, `.eve/`, `.output/`, `.doc-quarantine/` not scanned)
**Commit / ref**: `2817eb2` on `feat/model-registry-openai-luna`, working tree dirty (this pass's own fixes)
**Supersedes**: `reports/2026-08-12-doc-hygiene.md` (same day, earlier commit) — that report is now the historical record of the drift it found and fixed. This one starts from the tree it left behind.

## Executive Summary

**22 documentation artifacts**: 20 Markdown files, plus `.env.example` and
`docker-compose.yml` as config-as-doc. By state: **19 ACTIVE, 3 DRIFTED,
0 STALE, 0 DEPRECATED, 0 ORPHANED**.

**Overall posture.** This repo is now in the top few percent for documentation
health, and it got there in one day. The earlier pass found 4 DRIFTED and no
automation at all; since then `scripts/check-docs.ts` and
`.github/workflows/ci.yml` were added, `CHANGELOG.md` is generated from commit
subjects, and two prose guards went into
`whatsapp-bridge/test/anti-corruption-layer.test.js`. Every list that was a
hand-maintained mirror of the code — 47 bridge routes, 37 agent tools, 58 config
keys — is now checked on every push. Inline coverage is genuinely excellent
(~511 JSDoc blocks against ~336 exports in the agent; ~179 against ~116 in the
bridge; ~1,056 comment lines in the Go transport), and the prose explains *why*,
which no generator could produce.

**The finding that matters is what the automation did not cover.**
`README.md` described "a scheduled tic-tac-toe (`ttt`,
`agent/schedules/tictactoe.md`)" and a coordination dance between it and the
bridge console. That file does not exist, and
`agent/skills/tictactoe/SKILL.md` says plainly that the five-minute schedule
"was retired: it duplicated the bridge console… and it produced the same board
twice when two ticks overlapped." So one document described a subsystem that
another document, in the same repo, recorded as deleted — the identical shape as
the browser-removal drift the surface-gap audit found, in a corner nobody had
looked at. It survived **two** audits and a hygiene pass because every check
written so far covers routes, tools, config keys and disclaimers, and **a
schedule is none of those.**

The same false claim was in code, at `whatsapp-bridge/src/whatsapp.js:1295`,
where `consoleState()` justified its own existence by "the agent's tic-tac-toe
tool, which answers `ttt` on a schedule."

The other two findings are lower stakes: two historical reports carried no
status marker, so a reader opening `2026-08-12-surface-gap-audit.md` met a
blocker-severity findings table with nothing to say it was closed.

**Top-3 actions** — all done in this pass, not proposed:

1. **`README.md`: phantom schedule** — rewritten to describe the tool as it is
   (answers when asked) and to record that the schedule was retired and why.
   `whatsapp-bridge/src/whatsapp.js` likewise.
2. **Close the class, not the instance** — `checkAgentPaths()` added to
   `scripts/check-docs.ts`: every `` `agent/<kind>/<name>` `` a document names
   must be on disk. Verified to fail on the real defect before being called done.
3. **Two unmarked historical reports** — banners added to
   `2026-08-12-surface-gap-audit.md` (CLOSED, with the commits that closed it)
   and `2026-08-10-corpus-findings.md` (snapshot; its §7 subject was deleted).

A second round then implemented the three checks this report had left as
recommendations — transport routes, console commands, report status — and those
immediately found `/noop` dispatched by the console and documented nowhere, plus
a live report with no status marker. See *Automation Recommendations*.

## Documentation Inventory

| # | Path | Type | State | Conf. | Modified | Lines | Signal |
|---|---|---|---|---|---|---|---|
| 1 | `README.md` | markdown | **DRIFTED** | HIGH | 2026-08-12 | 892 | Phantom `agent/schedules/tictactoe.md`; describes a retired schedule as live — **fixed this pass** |
| 2 | `SPEC.md` | markdown | **DRIFTED** | MEDIUM | 2026-08-12 | 1267 | §2 mapping row names `agent/subagents/extract.ts`; §5.6 says it was built as a tool instead — **fixed this pass** |
| 3 | `reports/2026-08-12-surface-gap-audit.md` | markdown | **DRIFTED** | HIGH | 2026-08-12 | 411 | All 5 findings closed, no status marker — **fixed this pass** |
| 4 | `reports/2026-08-10-corpus-findings.md` | markdown | ACTIVE | MEDIUM | 2026-08-12 | 486 | §7's subject (`selectors.js` ACL) deleted 2 days later; citations still valid — **banner added** |
| 5 | `reports/2026-08-12-doc-hygiene.md` | markdown | ACTIVE | HIGH | 2026-08-12 | 359 | Carries its own RESOLVED banner |
| 6 | `CHANGELOG.md` | markdown | ACTIVE | HIGH | 2026-08-12 | 54 | Generated by `scripts/gen-changelog.ts`; current as of `2817eb2` |
| 7 | `CLAUDE.md` | markdown | ACTIVE | HIGH | 2026-08-12 | 397 | §5 exemption list matches `FRONTMATTER_EXEMPT` in code |
| 8 | `DISCLAIMER.md` | markdown | ACTIVE | HIGH | 2026-08-11 | 210 | Referenced by README as CLAUDE.md mandates |
| 9 | `HOWTO-TRANSPORT-SETUP.md` | markdown | ACTIVE | HIGH | 2026-08-12 | 580 | Owns the pairing routes; no phantom refs |
| 10 | `agent/instructions.md` | markdown | ACTIVE | HIGH | 2026-08-12 | 177 | Loaded verbatim as system prompt; frontmatter-exempt |
| 11–13 | `agent/schedules/*.md` (3) | markdown | ACTIVE | HIGH | 2026-08-11/12 | 37–56 | All three registered in the `eve build` manifest |
| 14–20 | `agent/skills/*/SKILL.md` (7) | markdown | ACTIVE | HIGH | 2026-08-11/12 | 86–313 | All seven in the manifest; `whatsapp` rewritten this morning |
| 21 | `.env.example` | config-doc | ACTIVE | HIGH | 2026-08-12 | 443 | 58 keys; forward and reverse checked by `docs:check` |
| 22 | `docker-compose.yml` | config-doc | ACTIVE | HIGH | 2026-08-12 | 373 | Dead `WA_QUIET_HOURS` removed in `857cac8`; guarded by `test/doc-surface.test.ts` |

**No STALE classifications.** The repo's entire git history is 2026-08-10 to
2026-08-12, so the 6-month window is inapplicable; every classification above
rests on a structural mismatch or a resolved reference, never on a date.

## Active Doc Sync Results

### `README.md`

| Check | Verdict | Detail |
|---|---|---|
| 1a. Description vs `package.json` | SKIP | Manifest carries no `description` |
| 1b. Install commands | PASS | `engines.node: 24.x` matches the documented runtime |
| 1c. Directory tree | SKIP | No ASCII tree present — nothing to drift |
| 1d. Badges | SKIP | None |
| 1e. Route table (47 routes) | PASS | Enforced by `docs:check` (`routes`) |
| 1f. DISCLAIMER pointer | PASS | Line 18, path correct for depth |
| 6a. Module references | **DRIFT → fixed** | `agent/schedules/tictactoe.md` does not exist |
| 6b. Behavioural claim | **DRIFT → fixed** | "the scheduled game stands down" — no schedule runs |

**Sync score**: 6/6 applicable checks passing after this pass (was 4/6).

### `SPEC.md`

| Check | Verdict | Detail |
|---|---|---|
| 4a. Tool completeness, both directions | PASS | Enforced by `docs:check` (`tools:missing`, `tools:phantom`) |
| 6a. Module references | **DRIFT → fixed** | `agent/subagents/extract.ts` in §2's table; §5.6 records it was never built |
| 6c. Section status markers | PASS | ✅/✖/📋 legend used consistently; §6.2 level 0.5 struck through in `857cac8` |

### `.env.example` / `docker-compose.yml`

| Check | Verdict | Detail |
|---|---|---|
| 5a. Env completeness (forward) | PASS | `docs:check` (`env:undocumented`) |
| 5a. Env completeness (reverse) | PASS | `test/doc-surface.test.ts` |
| 5c. Compose ↔ code alignment | PASS | `test/doc-surface.test.ts` — no service is handed a setting nothing reads |
| 5b. Documented defaults | PASS (spot-check) | `WA_TRANSPORT_DRAIN_INTERVAL_MS=5000` matches `transport.js` |

### `CHANGELOG.md`

| Check | Verdict | Detail |
|---|---|---|
| 3a. Latest version vs tag | SKIP | `version: 0.0.0`, no git tags — pre-release, honest |
| 3b. Unreleased section | PASS | All 20 commits present under `## Unreleased` |
| 3c. Automation | PASS | `scripts/gen-changelog.ts`, `npm run changelog` |

### CI / automation alignment (§8)

| Check | Verdict | Detail |
|---|---|---|
| 8a. Documented scripts run | PASS | Enforced twice — `docs:check` (`scripts`) and `test/doc-surface.test.ts` |
| 8b. CI does what docs say | PASS | `ci.yml` runs typecheck, `docs:check`, and all three suites |

## Quarantine Proposals

**None.** No doc is DEPRECATED, ORPHANED, or STALE-with-HIGH-confidence.

The two candidates a naive pass would flag are both deliberate and correctly
placed:

- `.doc-quarantine/deprecated/spec-draft.md` and
  `.doc-quarantine/orphaned/PRODUCT-DRAFT.md` — already quarantined by the
  earlier pass, with summaries in `.doc-quarantine/quarantine-summaries.md`.
- The three files in `reports/` are historical artifacts **by design**. They are
  dated, they now all carry status banners, and they hold reasoning that is not
  recoverable from the code. Retiring them would destroy the record of why the
  browser was removed. They stay.

No `doc-quarantine.sh` is emitted this pass, because generating an empty script
would imply there was something to review.

## Automation Recommendations

The earlier pass's recommendations were implemented, so this table is short by
design. What follows is what remains **unautomated after** that work.

**All four are implemented.** The table records what each replaces and what it
caught, because a check that has never caught anything is a check nobody should
trust.

| # | Doc | Element | Tool | Hook | What it caught when first run |
|---|---|---|---|---|---|
| 1 | any | Paths under `agent/` | `checkAgentPaths()` | CI + `npm run docs:check` | `agent/schedules/tictactoe.md` — a retired schedule documented across two audits |
| 2 | `README.md`, `HOWTO` | Go transport routes (20) | `checkTransportRoutes()` | Same | Nothing — all 20 were documented. Pure regression guard, and the asymmetry it removes was the risk: 47 of 67 routes checked *reads* as full coverage |
| 3 | `README.md` | Console slash-commands (6) | `checkConsoleCommands()` | Same | `/noop` — dispatched by the console, documented nowhere |
| 4 | `reports/*.md` | Status banners | `checkReportStatus()` | Same | `2026-08-12-context-architecture.md` — a live report with no marker |

Each carries a **self-guard**: if the transport's mux registration moves, if
`PLUGINS` changes shape, or if the README's menu fence is reworded, the check
says so instead of finding nothing and reporting a clean surface. That failure
mode — a check that passes because its extraction broke — is the one that
matters, because it is indistinguishable from success in CI.

**#3 was itself wrong on the first attempt, and the way it was caught is the
point.** It searched the whole README, so a passing mention of `` `/noop` `` in
prose satisfied it while the command was still missing from the menu block a
user actually reads. The bug surfaced only because each check was tested by
*re-introducing the defect it exists to catch* — remove `/noop` from the menu,
confirm the check fails, restore. It did not fail. A check written and never
seen to fail would have shipped green and worthless.

### Detailed notes

#### #1: assert every documented `agent/` path exists

**What**: `checkAgentPaths()` in `scripts/check-docs.ts`. Every
`` `agent/{schedules,skills,tools,hooks,channels,connections}/…` `` named in a
doc under `agent/` or in `README.md` / `SPEC.md` / `HOWTO-TRANSPORT-SETUP.md` /
`CLAUDE.md` must exist on disk.

**Why it generalises**: `agent/` is a manifest directory — eve registers what is
on disk — so a path in prose is a *claim about the built manifest*, and claims
about the manifest are checkable by looking.

**The escape hatch, deliberately**: a line that says the thing was removed,
retired, superseded or never built is allowed to name it. Recording what went is
how any removal gets explained; a check that forbade it would push people to
delete the explanation instead of the phantom. Same allowance the ACL prose
guards make.

**Verified, not assumed**: the phantom line was re-added to `README.md` and
`docs:check` was confirmed to fail on it with the exact path and line number,
then reverted. A check that has never been seen to fail is a check nobody should
trust.

#### #2: route checking extended to the Go transport

**What**: `checkRoutes()` read only `whatsapp-bridge/src/server.js`. The
transport's 20 routes, registered in `whatsapp-transport/internal/httpapi/api.go`,
were unchecked.

**How**: `checkTransportRoutes()` matches both `mux.Handle` and `mux.HandleFunc`,
splits Go's `"METHOD /path"` pattern, and asserts the path appears in README,
SPEC or HOWTO. The method is deliberately not required to be adjacent — a table
with the method in its own column is documentation too, and demanding otherwise
teaches people to write for the checker.

**Why it mattered even though nothing was undocumented**: the asymmetry was the
danger. 47 of 67 routes enforced makes `docs:check` passing read as full route
coverage, so the next transport endpoint would have been undocumented by default
with nothing to say so.

#### #3: the console command surface, checked both ways

**What**: six commands are dispatched from `PLUGINS` in
`whatsapp-bridge/src/plugins.js`, and the README prints a menu block listing
them. `/noop` was in the first list and not the second.

**How**: `checkConsoleCommands()` extracts `command: "/x"` from `plugins.js` and
compares against the fenced menu block in the README — both directions. A
command in the menu that the console does not dispatch is the sharper failure:
the user types it into their own chat and gets silence, and silence is exactly
what an ordinary note returns, so there is no error to see.

**The menu block, not the whole README** — see the note above about how the
first version of this check sat green while the defect was live.

## Quarantine Artifacts

None generated — see *Quarantine Proposals*. The earlier pass's artifacts remain
at `.doc-quarantine/quarantine-manifest.json` and
`.doc-quarantine/quarantine-summaries.md`.

## Methodology Notes

### What was inspected

- All non-hidden directories: `agent/`, `whatsapp-bridge/`,
  `whatsapp-transport/`, `scripts/`, `test/`, `evals/`, `reports/`, root.
- Stack: TypeScript (eve `0.31.3`, Node 24) + JavaScript (bridge) + Go
  (transport). No monorepo tool, no doc-site generator, no `docs/` directory.
- CI read: `.github/workflows/ci.yml`.
- Existing doc automation read in full: `scripts/check-docs.ts`,
  `scripts/gen-changelog.ts`.
- Git history: complete (2026-08-10 → 2026-08-12, 20 commits).
- Cross-reference sweep: every `` `path.ext` `` in every doc resolved against
  disk, with bridge-relative and history-marked mentions excluded by hand.

### What was NOT inspected

- `.repo/`, `.eve/`, `.output/`, `.doc-quarantine/`, `node_modules/`, `data/`
  (runtime state, private message content).
- Inline docstrings beyond the coverage estimate; no individual JSDoc block was
  classified.
- `evals/` internals — not a consumer-facing doc surface.
- Semantic accuracy of the Go transport's 1,056 comment lines.
- Whether each of the 47 bridge routes behaves as its table row says. Presence
  is checked; behaviour is not.

### Heuristic limitations

Static pattern matching, git metadata and structural comparison cannot detect:

- Prose that names the right file and explains the wrong behaviour. The
  tic-tac-toe finding was caught only because the claim happened to include a
  **path**; had it said "a scheduled game" with no path, nothing here would have
  found it.
- Docs held outside the repo.
- Accuracy of diagrams or embedded images.
- Usefulness. This measures freshness and referential integrity, not whether a
  document is worth reading.

One further limitation is specific to this pass: **three of the four
recommendations above were written by the same agent that wrote the checks they
grade.** An independent reading of `scripts/check-docs.ts` would be worth more
than this section's confidence in it.
