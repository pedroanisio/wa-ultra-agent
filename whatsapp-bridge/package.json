{
  "name": "whatsapp-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The archive and its HTTP API. Reception, sending, media and history run through whatsapp-transport (Go, whatsmeow); this service owns store.db and is its only writer.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=22"
  }
}
